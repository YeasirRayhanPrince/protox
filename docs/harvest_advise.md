Data-harvesting advice â the non-obvious traps that cost us re-harvest cycles

- The expensive part is never the bug itself â it's that everything downstream already consumed it before you noticed. So spend your effort up front: cheap invariant checks that run at harvest time and fail loud, before any consumer reads a record. And when you do find a defect, fix it at the producer and re-gate every corpus â never patch it downstream, or the bad records live on in the artifacts you already built.
- Persist RAW first, derive later. A fix to your parsing/derivation cannot be applied to records that only stored the derived value â that single mistake was our #1 cause of full re-harvests. Raw is cheap insurance.
- Stamp the full production regime on every record, and measure subject and baseline under identical conditions. A value without the conditions it was produced under is silently non-comparable to the next batch; a delta taken across mismatched conditions measures the conditions, not the thing you care about. (Corollary we relearned this week: contention on a shared host changes the numbers â record concurrency or isolate.)
- Validate against an INDEPENDENT oracle, never a target derived from the same output. Self-certifying checks pass on corrupted data â we scored "green" on records that were wrong.
- Treat "can't prove it" as refuse/quarantine, not accept â and treat a surprising magnitude as a measurement-bug suspect until proven. We once built a whole conclusion on a large effect that was just an input-construction bug.
- Decide the train/eval grouping key before harvesting and enforce it at every stage. Leakage discovered late (near-duplicates split across both sides) invalidates results and forces a re-split and re-harvest â and a filter that only runs at the first stage won't save you.
- One source of truth per fact. The same fact declared in several places drifts apart silently, and the mismatch surfaces as data that's subtly wrong for no visible reason.
- Record failures/timeouts as labeled outcomes, not missing values â keep why, or you can't tell "genuinely bad" from "broke mid-measurement" without re-running everything.